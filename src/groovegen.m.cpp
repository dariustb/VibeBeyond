#include <groovegen_elements.h>
#include <iostream>

using namespace std;

int main() {
    // 1. RNG Song Elements
    // srand(time(NULL));
    // Elements SongElements;

    // 2. Generate MIDI Loops

    // 3. Generate Audio Loops

    // 4. Generate Drum Loops

    // 5. Generate Song Segments

    // 6. Combine Segments into Final Audio

    // 7. Clean up

    cout << "Hello world!" << endl;

    return 0;
}
